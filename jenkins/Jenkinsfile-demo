@Library('pipeline-template@main') _

pipeline {
    agent any
    stages {
        stage('Demo') {
            steps {
                echo 'Hello world'
                sayHello 'Developers'
            }
        }
    }
}